```sql
WITH RankedStudentScores AS (
    SELECT
        c.Name AS CourseName,
        su.FirstName AS StudentFirstName,
        su.LastName AS StudentLastName,
        ss.Mark,
        ROW_NUMBER() OVER (PARTITION BY ss.CourseId ORDER BY ss.Mark DESC) AS StudentRank
    FROM
        dbo.StudentScore AS ss
    INNER JOIN
        dbo.Courses AS c ON ss.CourseId = c.Id
    INNER JOIN
        dbo.Users AS u ON ss.StudentId = u.Id
    INNER JOIN
        dbo.SystemUsers AS su ON u.SystemUserId = su.Id
    WHERE
        ss.IsDeleted = 0
        AND c.IsDeleted = 0
)
SELECT
    CourseName,
    StudentFirstName,
    StudentLastName,
    Mark
FROM
    RankedStudentScores
WHERE
    StudentRank <= 3
ORDER BY
    CourseName ASC,
    StudentRank ASC;
```

**Sample Rows:**

| CourseName  | StudentFirstName | StudentLastName | Mark |
| :---------- | :--------------- | :-------------- | :--- |
| 123         | Rana             | Ahmed           | 50.0 |
| 123         | Koky             | Aly             | 50.0 |
| 123         | Koky             | Aly             | 50.0 |
| 123456789   | aaaaaaaaaaaa     | aaaaaa          | 50.0 |
| 123456789   | ApIs             | radwa           | 25.0 |

**Summary:**

The query successfully retrieves the top 3 scoring students for each course. It first ranks students within each course based on their `Mark` in descending order, considering only active scores and courses. Then, it filters these results to include only those students with a rank of 3 or less.

The sample data shows:
*   For the course "123", three students (Rana Ahmed and Koky Aly) are listed with a mark of 50.0. Note that Koky Aly appears twice, suggesting multiple entries for the same student in the top 3, possibly due to multiple scores or ties.
*   For the course "123456789", two students are shown: "aaaaaaaaaaaa aaaaaa" with a mark of 50.0 and "ApIs radwa" with a mark of 25.0.

The query returned a total of 557 rows, with 5 rows displayed in the sample.